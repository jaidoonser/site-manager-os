# All of these ship with the reference environment already; listed here so
# `pip install -r requirements.txt` works on a machine that doesn't have them.
Flask>=3.0
Werkzeug>=3.0
pypdf>=3.17
openpyxl>=3.1
reportlab>=4.0
Pillow>=10.0
